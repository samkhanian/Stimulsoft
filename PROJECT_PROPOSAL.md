# 🚀 پروژه نمونه: دفتر معین هوشمند (Smart Financial Ledger)

این پروژه کوچک برای نمایش عملی معماری هیبریدی و استراتژی محاسبات سه‌لایه طراحی شده است.

## 📝 شرح پروژه
یک سیستم مدیریت تراکنش‌های مالی که شامل یک صفحه اصلی با **Kendo Grid** و یک دکمه **چاپ گزارش** (Stimulsoft) است. هر دو بخش از یک سرویس مرکزی در بک‌ند تغذیه می‌کنند.

---

## 🏗️ نمایش عملی لایه‌های محاسبات

### ۱. لایه دیتابیس (Persisted Totals)
- **سناریو:** "مانده از قبل" (Opening Balance) هر حساب.
- **پیاده‌سازی:** این مقدار مستقیماً از جدول `Accounts` خوانده می‌شود. طراح گزارش (Designer) فیلد `OpeningBalance` را به عنوان یک فیلد قطعی در Schema می‌بیند.

### ۲. لایه بک‌ند (Business Logic)
- **سناریو:** محاسبه "ارزش افزوده (VAT)" و "تبدیل واحد پولی".
- **پیاده‌سازی:** یک `FinancialEngine` در .NET، مقدار خام را گرفته، مالیات را بر اساس قوانین بیزینس محاسبه کرده و در DTO قرار می‌دهد.
- **نتیجه:** عددی که در گرید Kendo می‌بینید، دقیقاً همان عددی است که در خروجی PDF گزارش چاپ می‌شود.

### ۳. لایه فرانت‌بند (UX Performance)
- **سناریو:** "جمع ردیف‌های انتخاب شده" در گرید.
- **پیاده‌سازی:** با استفاده از توابع داخلی **Kendo UI Aggregate**، وقتی کاربر چند ردیف را تیک می‌زند، جمع آن‌ها در لحظه در فوتر گرید نشان داده می‌شود.
- **علت:** نیازی به درگیر کردن سرور برای یک محاسبه نمایشی و لحظه‌ای نیست.

---

## 🛠️ تسک‌های پیاده‌سازی (PVP - Prototyping Value Points)

1. **مدل داده (DTO):** طراحی `TransactionDto` شامل:
   - `Id`, `Date` (Miladi/Shamsi)
   - `Amount` (خام)
   - `TaxAmount` (محاسبه شده در BE)
   - `FinalAmount` (محاسبه شده در BE)
   - `OpeningBalance` (خوانده شده از DB)

2. **بک‌ند (.NET):** 
   - ایجاد `TransactionController` با یک Endpoint که لیست DTOها را برمی‌گرداند.
   - ایجاد `ReportController` که فایل MRT خنثی را لود کرده و همان لیست DTOها را به آن Push می‌کند.

3. **فرانت‌بند (Angular):**
   - نمایش لیست در `kendo-grid`.
   - استفاده از `kendo-pdf-export` یا ویوئر Stimulsoft برای نمایش خروجی نهایی.

---

## 📅 مدیریت تقویم (Common Utility)
- بک‌ند تاریخ را به صورت `DateTime` ارسال می‌کند اما در DTO یک فیلد `PersianDate` (رشته‌ای) نیز قرار می‌دهد تا طراح گزارش Stimulsoft بدون نیاز به اسکریپت‌نویسی، تاریخ شمسی را نمایش دهد.
- در فرانت‌بند، از یک Pipe آنگولار برای نمایش زیبای تاریخ استفاده می‌شود.
